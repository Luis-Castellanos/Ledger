/*
 * localStorage key for next-themes. Bumped from "vault:theme" when the app
 * went light-first so existing users pick up the new bright default.
 */
export const THEME_STORAGE_KEY = "ledger:theme";
