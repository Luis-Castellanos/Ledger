import { MigrationPlaceholder } from "@/components/migration-placeholder";

export default function PayrollPage() {
  return (
    <MigrationPlaceholder
      active="Payroll"
      description="Payroll will be ported from Gringotts with paystub storage, paycheck breakdowns, tax/benefit lines, and payroll-specific review workflows."
      title="Payroll"
    />
  );
}
